import React, { Fragment } from "react";
import "./styles.css";
import Clock from "../../components/Clock";

const Header = () => (
  <Fragment>
    <header id="main-header">Git App</header>;<Clock />
  </Fragment>
);

export default Header;
